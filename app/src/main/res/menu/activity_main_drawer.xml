<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- one item within this group will appear "selected" when we're in its page -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_receipt"
            android:icon="@drawable/ic_menu_receipt"
            android:title="@string/label_home" />
        <item
            android:id="@+id/nav_categories"
            android:icon="@drawable/ic_menu_categories"
            android:title="@string/label_categories" />
        <item
            android:id="@+id/nav_search"
            android:icon="@drawable/ic_menu_search"
            android:title="@string/label_search" />
    </group>

    <item
        android:id="@+id/nav_settings"
        android:icon="@drawable/ic_menu_settings"
        android:title="@string/label_settings" />

    <!-- assume user's logged in first (past startup screen)
        or should we just have logout in the settings menu?
     -->
    <item
        android:id="@+id/nav_logout"
        android:icon="@drawable/ic_menu_user"
        android:title="@string/label_logout" />

</menu>
